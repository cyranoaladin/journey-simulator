name: Verify

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  backend:
    name: Backend tests
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    defaults:
      run:
        working-directory: mf-back
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: mf-back/package-lock.json
      - name: Upgrade npm
        run: npm install -g npm@11
      - name: Install dependencies
        run: npm ci
      - name: Generate Prisma Client
        run: npx prisma generate
      - name: Push Database Schema
        env:
          DATABASE_URL: postgresql://test_user:test_password@localhost:5432/test_db?schema=public
        run: npx prisma db push --accept-data-loss
      - name: Run tests
        env:
          DATABASE_URL: postgresql://test_user:test_password@localhost:5432/test_db?schema=public
        run: npm test

  frontend:
    name: Frontend lint & unit tests
    runs-on: ubuntu-latest
    needs:
      - backend
    defaults:
      run:
        working-directory: journey-simulator
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: journey-simulator/package-lock.json
      - name: Upgrade npm
        run: npm install -g npm@11
      - name: Install dependencies
        run: npm ci
      - name: Lint & unit tests
        run: npm run check
      - name: Build
        run: npm run build

  playwright-smoke:
    name: Playwright smoke
    runs-on: ubuntu-latest
    needs:
      - frontend
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    defaults:
      run:
        working-directory: journey-simulator
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: journey-simulator/package-lock.json
      - name: Upgrade npm
        run: npm install -g npm@11
      
      - name: Install backend dependencies
        working-directory: mf-back
        run: npm ci
      - name: Generate Prisma Client
        working-directory: mf-back
        run: npx prisma generate
      - name: Push Database Schema
        working-directory: mf-back
        env:
          DATABASE_URL: postgresql://test_user:test_password@localhost:5432/test_db?schema=public
        run: npx prisma db push --accept-data-loss
      
      - name: Build backend
        working-directory: mf-back
        run: npm run build      - name: Start backend server
        working-directory: mf-back
        env:
          DATABASE_URL: postgresql://test_user:test_password@localhost:5432/test_db?schema=public
          PORT: 3002
          NODE_ENV: test
          JWT_SECRET: test-jwt-secret-key-for-ci
        run: |
          echo "Starting backend with DATABASE_URL=$DATABASE_URL"
          npm start > backend.log 2>&1 &
          echo $! > backend.pid
          echo "Backend PID: $(cat backend.pid)"
      
      - name: Wait for backend
        run: |
          echo "Waiting for backend to be ready..."
          for i in {1..30}; do
            if curl -f http://127.0.0.1:3002/health 2>/dev/null || curl -f http://127.0.0.1:3002/ 2>/dev/null; then
              exit 0
            fi
            echo "Attempt $i/30: Backend not ready yet..."
            sleep 2
          done
          echo "Backend failed to start within 60 seconds"
          echo "=== Backend logs ==="
          cat mf-back/backend.log || echo "No backend log found"
          echo "=== Backend process status ==="
          ps aux | grep node || echo "No node processes found"
          exit 1
      
      - name: Install frontend dependencies
        run: npm ci
      - name: Build frontend
        run: npm run build
      - name: Start frontend server
        run: |
          npm run preview -- --port 3000 --host 0.0.0.0 > frontend.log 2>&1 &
          echo $! > frontend.pid
      - name: Wait for frontend
        run: |
          echo "Waiting for frontend to be ready..."
          for i in {1..30}; do
            if curl -f http://127.0.0.1:3000/ 2>/dev/null; then
              echo "Frontend is ready!"
              exit 0
            fi
            echo "Attempt $i/30: Frontend not ready yet..."
            sleep 2
          done
          echo "Frontend failed to start within 60 seconds"
          cat frontend.log || true
          exit 1
      - name: Install Playwright browsers
        env:
          DATABASE_URL: postgresql://test_user:test_password@localhost:5432/test_db?schema=public
        run: npx playwright install --with-deps chromium
      - name: Run smoke tests
        env:
          CI: 'true'
          DATABASE_URL: postgresql://test_user:test_password@localhost:5432/test_db?schema=public
        run: npm run test:e2e:smoke
      - name: Upload Playwright artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-smoke-report
          path: |
            journey-simulator/tests/e2e-report
            journey-simulator/test-results
          if-no-files-found: ignore

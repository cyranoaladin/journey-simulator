// Minimal stubs for @solana/web3.js used in components
export class PublicKey {
  static unique() {
    return new PublicKey();
  }
  toString() {
    return "PublicKeyStub";
  }
}
export const clusterApiUrl = (_: any) => "";
export class Transaction {}

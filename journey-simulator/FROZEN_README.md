# FROZEN — Reference Only

This directory contains a previous iteration of the simulator with its own `web/` and `mf-back/`. The authoritative MVP for the investor demo is consolidated in the top‑level `web/`. Keep this folder read‑only unless a task explicitly targets it.
